# Tasks

> References: requirements in `specs/` (`product-listing-api`, `api-error-handling`, `api-configuration`, `catalog-bootstrap`, `api-request-logging`); decisions in `design.md` (D1–D12). Language: code, comments, test names and messages in **English**; these tasks in Portuguese. Do not commit (the user reviews and commits). **No new dependency** (if something requires one, pause and ask). Temporary verification files go in the scratchpad, never in the repository. Every test follows the Definition of Done in `CLAUDE.md`: observable behavior, deterministic, without mocking what is under test.

## 1. Shared contracts (`packages/shared`)

- [x] 1.1 Create `src/constants.ts` (`DEFAULT_LIMIT` 30, `MAX_LIMIT` 100, `MAX_SEARCH_LENGTH` 100) and `src/errors.ts` (`ERROR_CODES` list with the six codes, `ErrorCode` type, envelope's `apiErrorSchema` with optional `details` `{ path, message }`) with tests: the contract lists exactly the six codes; the schema accepts an envelope with and without `details` and rejects an unknown code, missing `message` and malformed `details`. Verify with `npm test -w @vynyl/shared` (new tests pass).
- [x] 1.2 Create `src/product.ts` with `productSchema` and the `Product` type (rules from section 6 of the guide; `price` with `multipleOf(0.01)`; lowercase `category`; `meta.*` with `z.iso.datetime()`), with tests: accepts the statement's product 1 and prices `19.99`, `4.35` and `0`; rejects, one rule per test, empty title and one with 201 characters, description with 2001, category with an uppercase letter, negative price/`1.005`/`NaN`, negative or non-integer stock, empty brand, `sku` outside the pattern or too short, weight `0` and negative, and invalid dates in `meta`. Verify with the `shared` tests.
- [x] 1.3 Create `src/list-products.ts` with `listProductsQuerySchema` (`z.strictObject`; `limit` and `offset` as `^\d+$` text converted and bounded, defaults 30 and 0; `q` with `trim`, maximum 100 and empty treated as absent), the `ListProductsQuery` type, `productListResponseSchema` and `ProductListResponse`, with tests: defaults on `{}`; accepts `limit=1` and `limit=100`, `offset=0`; rejects `limit` `0`, `101`, `abc`, `1.5`, `1e2`, `+5`, empty and with a space, `offset` `-1` and `abc`, repeated parameter (array), unknown key and `q` with 101 characters; `q` with spaces becomes the trimmed text and only spaces becomes absent; the paginated response validates `{ data, total, limit, offset }`. Verify with the `shared` tests.
- [x] 1.4 Export everything in `src/index.ts` (replacing the placeholder) and run `npm run typecheck`, `npm run lint` and `npm test -w @vynyl/shared`. Verify exit code 0 in the three and that the API and the web still compile (`npm run typecheck` at the root).

## 2. API core: errors, configuration, paths and logs

- [x] 2.1 Create `apps/api/src/lib/errors.ts` (`AppError` with `code`, derived `status`, `details` and `cause`; exhaustive `Record<ErrorCode, number>` map) with tests: each code maps to the guide's status (400, 404, 404, 409, 429, 500), `details` is preserved and `cause` propagated. Verify with `npm test -w @vynyl/api`; check that removing a code from the map **breaks** `typecheck` (temporary verification, reverted).
- [x] 2.2 Create `apps/api/src/config/env.ts` (`loadConfig(env)` with defaults, `ConfigError` whose message lists each invalid variable with the reason) with tests: defaults without variables; values provided; `PORT` `abc`, `0`, `70000` and empty; `LOG_LEVEL=verbose`; `NODE_ENV=staging`; several invalid together list all; the message contains no stack. Verify with the `api` tests.
- [x] 2.3 Create `apps/api/src/config/paths.ts` (`getPaths(importMetaUrl)` returning `repoRoot`, `envFile`, `migrationsDir`, `datasetFile`) with a test using the real path of `apps/api/src/server.ts`: `repoRoot` contains the root `package.json` (`name: vynyl`), `migrationsDir` contains `meta/_journal.json` and `datasetFile` exists. Verify with the `api` tests.
- [x] 2.4 Create `apps/api/src/lib/logger.ts` (`createLogger({ level, destination? })`) and `apps/api/src/middleware/request-logger.ts` (D9: `genReqId`, allowlist serializers, levels by status) with tests via Supertest and an in-memory stream: `X-Request-Id` generated (UUID), reused (`client-123`) and replaced when invalid (the invalid value does not appear in the log); the line is JSON with `req.id`, `req.method`, `req.url`, `res.statusCode` and `responseTime`; levels `info` (200), `warn` (400) and `error` (500); with `Authorization: Bearer segredo` and `Cookie: session=abc` no line contains `segredo` or `session=abc`; with level `silent` nothing is written. Verify with the `api` tests.

## 3. Database, mapper and seed

- [x] 3.1 Create `apps/api/src/db/client.ts` (`toDatabaseUrl`, `createDatabase` with parent-folder creation, pragmas and `Db` type), `apps/api/src/db/migrate.ts` (`runMigrations`) and the test helper `apps/api/src/test/database.ts` (`createTestDatabase`: `:memory:` + migrations, with `close`) with tests: `toDatabaseUrl` for `:memory:`, relative (against the root), absolute and a path with a space; `createDatabase` creates the nonexistent folder; on a temporary file the journal is `wal`, `PRAGMA foreign_keys` is 1 and `PRAGMA busy_timeout` is set; on `:memory:` it opens and `foreign_keys` is 1; `runMigrations` creates `products` on a new database and reapplying changes no data and throws no error. Verify with the `api` tests (if `foreign_keys` is not 1 on `:memory:`, pause and report).
- [x] 3.2 Create `apps/api/src/mappers/product.mapper.ts` (`toProduct`, `toProductInsert`) with tests: `999→9.99`, `1999→19.99`, `29→0.29`, `435→4.35`, `0→0`; round trip (`9.99`, `19.99`, `0.29`, `4.35`, `1299`, `0.01`) preserves the value; nested `meta` with `createdAt`/`updatedAt`; no internal field (`priceCents`, `created_at`) in the output. Verify with the `api` tests.
- [x] 3.3 Create `apps/api/src/db/seed.ts` (`seedProducts(db, { file, logger })`, D5) with tests against an in-memory database and the **real data set**: inserts all products (count read from the file) preserving `id`, fields and timestamps; a second run inserts nothing and returns `skipped`; a product with negative `stock` fails indicating index and field and leaves the table empty; a repeated `sku` rolls everything back (empty table); malformed JSON fails citing the file; prices `19.99`, `0.29` and `4.35` end up as 1999, 29 and 435 cents; after the seed, an `INSERT` without `id` receives the largest `id` + 1. Verify with the `api` tests.

## 4. Endpoint layers

- [x] 4.1 Create `apps/api/src/lib/like.ts` (`escapeLikePattern`) and `apps/api/src/repositories/products.repository.ts` (`ProductsRepository` + `createProductsRepository(db)`, D10) with tests: `escapeLikePattern` for `\`, `%`, `_`, a mix and plain text unchanged; repository with an **in-memory database and own fixtures** (includes titles with literal `%` and `_` and different cases): ordering by ascending `id`, `limit`/`offset`, `total` equal on all pages, `offset` beyond the end (empty rows and correct `total`), search by title **or** description, ASCII case-insensitive, literal `q=%` and `q=_`, search + pagination combination, no results. Verify with the `api` tests.
- [x] 4.2 Create `apps/api/src/services/products.service.ts` (`ProductsService` + `createProductsService({ productsRepository })`) with tests using a **fake** repository: forwards `limit`, `offset` and `q` (absent when there is no search), converts the rows with the mapper (decimal price), builds `{ data, total, limit, offset }` and propagates the repository's error unchanged. Verify with the `api` tests.
- [x] 4.3 Create `apps/api/src/middleware/validate.ts` (`validate({ query, params, body })` and `getValidated`, D7) with tests in a minimal Express app: valid query, params and body arrive converted in `res.locals.validated` and `req.query` stays intact; invalid ones call `next` with `AppError` `VALIDATION_ERROR` and `details` with the field's `path`; a root issue (unknown key) has an empty `path`; a source not provided is not validated. Verify with the `api` tests.
- [x] 4.4 Create `apps/api/src/middleware/not-found.ts` and `apps/api/src/middleware/error-handler.ts` (D8) with tests in a minimal app: `AppError` becomes the envelope with the code's status (with and without `details`); a plain error becomes `500 INTERNAL_ERROR` with a generic message, without the original `message`, SQL or stack in the body, and the `error`-level log contains the full error with the response's `X-Request-Id`; an `async` handler that rejects reaches central handling; with headers already sent it delegates with `next(err)`; any route with no match (GET and POST) receives `404 NOT_FOUND`. Verify with the `api` tests.
- [x] 4.5 Create `apps/api/src/handlers/products.handler.ts` and `apps/api/src/routes/products.routes.ts` with a handler test using a **fake** service: uses the values from `res.locals.validated.query`, calls the service once and responds `200` with the result; the `GET /` route applies `validate` before the handler. Verify with the `api` tests and `npm run typecheck -w @vynyl/api`.

## 5. Application and bootstrap

- [x] 5.1 Create `apps/api/src/app.ts` (`createApp({ db, logger })`: `x-powered-by` disabled, request logger, `/api/products`, not-found and error-handler, in that order) and the helper `apps/api/src/test/app.ts`, with route tests via Supertest and the real data set covering **all the scenarios** of the specs `product-listing-api` (30 default and `id` 1–30, `total` 44, representation of product 1, decimal price, pages `limit=10&offset=10`, `offset=30`, `limit=100`, `offset=1000`, empty catalog, each 400 and the parameter's `details`, search `flux`/`FLUX`, description only, search + pagination, no results, `q=%25`, `q` empty and with spaces), `api-error-handling` (404 on `/api/does-not-exist` and `/qualquer-coisa`, envelope and `Content-Type`, real 500 by closing the `client` before the request without leaking the error text) and `api-request-logging` (`X-Request-Id` header, absence of `x-powered-by`). Verify with `npm test -w @vynyl/api` and map each spec scenario to at least one test (list the mapping in the report).
- [x] 5.2 Implement `apps/api/src/server.ts` as the composition root (D2/D3): `getPaths` → optional `.env` via `process.loadEnvFile` (ignores `ENOENT`, does not overwrite the environment) → `loadConfig` (on `ConfigError`, writes the message to `stderr` and exits with code 1 before touching the database) → `createLogger` → `createDatabase` → `runMigrations` → `seedProducts` → `createApp` → `listen` (`info` log with the port); startup failures are recorded at `fatal` level and exit with code 1. Remove the `.gitkeep` files from the `config`, `routes`, `handlers`, `services`, `repositories`, `mappers`, `middleware` and `lib` folders (now with content). Verify with `npm run typecheck`, `npm run lint` and `npm test` at the root, and that no `.gitkeep` remains in folders with files.

## 6. Runtime verification (the server is run, not just tested)

- [x] 6.1 Start the development server (`tsx`) with an absolute `DATABASE_PATH` in a scratchpad temporary directory, a free `PORT` and `LOG_LEVEL=info`, and call with `fetch`/`curl`: `GET /api/products` (30 items, `total` 44), `?limit=10&offset=10`, `?q=flux`, `?q=%25` (0 results), `?limit=0` (400 with envelope and `details`), `GET /api/nope` (404), and the `X-Request-Id` header; show the real results and a log line. End the process and confirm the port is free.
- [x] 6.2 Restart the server over the **same** database file and verify that `total` stays equal to the data set file's (no duplication) and that the log records the seed as skipped; compare the count via `GET /api/products?limit=100`.
- [x] 6.3 Run `npm run build -w @vynyl/api` and run `node apps/api/dist/server.js` **from `apps/api` and then from the root**, with `DATABASE_PATH=./data/t2-cwd-check.db`: confirm that both use the **same** file in `<root>/data/` (and that no `apps/api/data/` appears), that `dist/server.js` contains no imports of `@vynyl/shared` (embedded) and that the `GET /api/products` response is the same as in dev mode. Remove the temporary database files.
- [x] 6.4 Invalid configuration: start with `PORT=abc` and then with `PORT=abc LOG_LEVEL=verbose`; verify a non-zero exit code, a message with no stack that names each invalid variable and that **no** database file was created.
- [x] 6.5 Optional `.env`: first confirm that there is no `.env` at the root (if there is, pause); create a temporary `.env` with `PORT` and verify that the API listens on that port; define a different `PORT` in the environment and verify that the environment takes precedence; remove the temporary `.env` and confirm that the API starts normally without it.

## 7. Closing

- [x] 7.1 Run at the root `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` and `npm run format`; verify exit code 0 in all, that `format` run twice does not change files and record the count of files and tests per package.
- [x] 7.2 Check the scope: `git status` only with expected files; each workspace's `package.json` and `package-lock.json` **unchanged** (no new dependency); no out-of-scope code (`express.json`, rate limit, `/health`, `SIGTERM`, singleflight, sorting, categories); no `console.*`, TODO, temporary file or versionable `data/*.db`; `README.md`, `PROJECT_GUIDE.md`, `CLAUDE.md` and `DELIVERABLES.md` unchanged.
- [x] 7.3 Run `openspec validate api-list-products --strict` and verify that it passes. Reflect the progress in `OPENSPEC_TASKS.md`: check `[x]` only the scope and acceptance criteria items of T2 **actually verified**, **without** checking T2 in the Overall status or the "Closing" (review, "Approved patterns", commit and `AI.md` notes belong to the user).
- [x] 7.4 Deliver the final report in the `CLAUDE.md` format: what was done, commands executed and real results, the scenario→test mapping, what was **not** verified, decisions and assumptions (including the optional `.env` and the single product schema), observed risks and the list of the **review points** from the `design.md` section for the user to approve the patterns; suggest the commit message `feat(api): add paginated product listing with search`. **Do not commit or push.**
