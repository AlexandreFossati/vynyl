# Tasks

> Referências: requisitos em `specs/` (`product-listing-api`, `api-error-handling`, `api-configuration`, `catalog-bootstrap`, `api-request-logging`); decisões em `design.md` (D1–D12). Idioma: código, comentários, nomes de testes e mensagens em **inglês**; estas tasks em português. Não fazer commit (o usuário revisa e commita). **Nenhuma dependência nova** (se algo exigir uma, pausar e perguntar). Arquivos temporários de verificação ficam no scratchpad, nunca no repositório. Todo teste segue a Definition of Done do `CLAUDE.md`: comportamento observável, determinístico, sem mockar o que está sob teste.

## 1. Contratos compartilhados (`packages/shared`)

- [x] 1.1 Criar `src/constants.ts` (`DEFAULT_LIMIT` 30, `MAX_LIMIT` 100, `MAX_SEARCH_LENGTH` 100) e `src/errors.ts` (lista `ERROR_CODES` com os seis códigos, tipo `ErrorCode`, `apiErrorSchema` do envelope com `details` opcional `{ path, message }`) com testes: o contrato lista exatamente os seis códigos; o schema aceita envelope com e sem `details` e rejeita código desconhecido, `message` ausente e `details` malformado. Verificar com `npm test -w @vynyl/shared` (novos testes passam).
- [x] 1.2 Criar `src/product.ts` com `productSchema` e o tipo `Product` (regras da seção 6 do guia; `price` com `multipleOf(0.01)`; `category` minúscula; `meta.*` com `z.iso.datetime()`), com testes: aceita o produto 1 do enunciado e preços `19.99`, `4.35` e `0`; rejeita, uma regra por teste, título vazio e com 201 caracteres, descrição com 2001, categoria com maiúscula, preço negativo/`1.005`/`NaN`, estoque negativo ou não inteiro, marca vazia, `sku` fora do padrão ou curto, peso `0` e negativo, e datas inválidas em `meta`. Verificar com os testes do `shared`.
- [x] 1.3 Criar `src/list-products.ts` com `listProductsQuerySchema` (`z.strictObject`; `limit` e `offset` como texto `^\d+$` convertido e limitado, padrões 30 e 0; `q` com `trim`, máximo 100 e vazio tratado como ausente), o tipo `ListProductsQuery`, `productListResponseSchema` e `ProductListResponse`, com testes: defaults em `{}`; aceita `limit=1` e `limit=100`, `offset=0`; rejeita `limit` `0`, `101`, `abc`, `1.5`, `1e2`, `+5`, vazio e com espaço, `offset` `-1` e `abc`, parâmetro repetido (array), chave desconhecida e `q` com 101 caracteres; `q` com espaços vira o texto aparado e só espaços vira ausente; a resposta paginada valida `{ data, total, limit, offset }`. Verificar com os testes do `shared`.
- [x] 1.4 Exportar tudo em `src/index.ts` (substituindo o placeholder) e executar `npm run typecheck`, `npm run lint` e `npm test -w @vynyl/shared`. Verificar código de saída 0 nos três e que a API e o web ainda compilam (`npm run typecheck` na raiz).

## 2. Núcleo da API: erros, configuração, caminhos e logs

- [x] 2.1 Criar `apps/api/src/lib/errors.ts` (`AppError` com `code`, `status` derivado, `details` e `cause`; mapa exaustivo `Record<ErrorCode, number>`) com testes: cada código mapeia o status do guia (400, 404, 404, 409, 429, 500), `details` é preservado e `cause` propagado. Verificar com `npm test -w @vynyl/api`; conferir que remover um código do mapa **quebra** o `typecheck` (verificação temporária, revertida).
- [x] 2.2 Criar `apps/api/src/config/env.ts` (`loadConfig(env)` com padrões, `ConfigError` cuja mensagem lista cada variável inválida com o motivo) com testes: defaults sem variáveis; valores informados; `PORT` `abc`, `0`, `70000` e vazio; `LOG_LEVEL=verbose`; `NODE_ENV=staging`; várias inválidas juntas listam todas; a mensagem não contém stack. Verificar com os testes do `api`.
- [x] 2.3 Criar `apps/api/src/config/paths.ts` (`getPaths(importMetaUrl)` devolvendo `repoRoot`, `envFile`, `migrationsDir`, `datasetFile`) com teste usando o caminho real de `apps/api/src/server.ts`: `repoRoot` contém o `package.json` raiz (`name: vynyl`), `migrationsDir` contém `meta/_journal.json` e `datasetFile` existe. Verificar com os testes do `api`.
- [x] 2.4 Criar `apps/api/src/lib/logger.ts` (`createLogger({ level, destination? })`) e `apps/api/src/middleware/request-logger.ts` (D9: `genReqId`, serializers em lista de permissão, níveis por status) com testes via Supertest e um stream em memória: `X-Request-Id` gerado (UUID), aproveitado (`client-123`) e substituído quando inválido (o valor inválido não aparece no log); a linha é JSON com `req.id`, `req.method`, `req.url`, `res.statusCode` e `responseTime`; níveis `info` (200), `warn` (400) e `error` (500); com `Authorization: Bearer segredo` e `Cookie: session=abc` nenhuma linha contém `segredo` nem `session=abc`; com nível `silent` nada é escrito. Verificar com os testes do `api`.

## 3. Banco de dados, mapper e seed

- [x] 3.1 Criar `apps/api/src/db/client.ts` (`toDatabaseUrl`, `createDatabase` com criação da pasta pai, pragmas e tipo `Db`), `apps/api/src/db/migrate.ts` (`runMigrations`) e o ajudante de teste `apps/api/src/test/database.ts` (`createTestDatabase`: `:memory:` + migrations, com `close`) com testes: `toDatabaseUrl` para `:memory:`, relativo (contra a raiz), absoluto e caminho com espaço; `createDatabase` cria a pasta inexistente; em arquivo temporário o journal é `wal`, `PRAGMA foreign_keys` vale 1 e `PRAGMA busy_timeout` está definido; em `:memory:` abre e `foreign_keys` vale 1; `runMigrations` cria `products` em banco novo e reaplicar não altera dados nem lança erro. Verificar com os testes do `api` (se `foreign_keys` não valer 1 em `:memory:`, pausar e reportar).
- [x] 3.2 Criar `apps/api/src/mappers/product.mapper.ts` (`toProduct`, `toProductInsert`) com testes: `999→9.99`, `1999→19.99`, `29→0.29`, `435→4.35`, `0→0`; ida e volta (`9.99`, `19.99`, `0.29`, `4.35`, `1299`, `0.01`) preserva o valor; `meta` aninhado com `createdAt`/`updatedAt`; nenhum campo interno (`priceCents`, `created_at`) na saída. Verificar com os testes do `api`.
- [x] 3.3 Criar `apps/api/src/db/seed.ts` (`seedProducts(db, { file, logger })`, D5) com testes contra banco em memória e o **data set real**: insere todos os produtos (contagem lida do arquivo) preservando `id`, campos e carimbos; segunda execução não insere nada e devolve `skipped`; produto com `stock` negativo falha indicando índice e campo e deixa a tabela vazia; `sku` repetido desfaz tudo (tabela vazia); JSON malformado falha citando o arquivo; preços `19.99`, `0.29` e `4.35` ficam em 1999, 29 e 435 centavos; após o seed, um `INSERT` sem `id` recebe o maior `id` + 1. Verificar com os testes do `api`.

## 4. Camadas do endpoint

- [x] 4.1 Criar `apps/api/src/lib/like.ts` (`escapeLikePattern`) e `apps/api/src/repositories/products.repository.ts` (`ProductsRepository` + `createProductsRepository(db)`, D10) com testes: `escapeLikePattern` para `\`, `%`, `_`, mistura e texto comum inalterado; repository com **banco em memória e fixtures próprias** (inclui títulos com `%` e `_` literais e caixas diferentes): ordem por `id` crescente, `limit`/`offset`, `total` igual em todas as páginas, `offset` além do fim (linhas vazias e `total` correto), busca por título **ou** descrição, insensível a caixa ASCII, `q=%` e `q=_` literais, combinação busca + paginação, sem resultados. Verificar com os testes do `api`.
- [x] 4.2 Criar `apps/api/src/services/products.service.ts` (`ProductsService` + `createProductsService({ productsRepository })`) com testes usando um repository **fake**: repassa `limit`, `offset` e `q` (ausente quando não há busca), converte as linhas com o mapper (preço decimal), monta `{ data, total, limit, offset }` e propaga o erro do repository sem alterá-lo. Verificar com os testes do `api`.
- [x] 4.3 Criar `apps/api/src/middleware/validate.ts` (`validate({ query, params, body })` e `getValidated`, D7) com testes num app Express mínimo: query, params e body válidos chegam convertidos em `res.locals.validated` e `req.query` permanece intacto; inválidos chamam `next` com `AppError` `VALIDATION_ERROR` e `details` com `path` do campo; issue na raiz (chave desconhecida) tem `path` vazio; origem não informada não é validada. Verificar com os testes do `api`.
- [x] 4.4 Criar `apps/api/src/middleware/not-found.ts` e `apps/api/src/middleware/error-handler.ts` (D8) com testes num app mínimo: `AppError` vira o envelope com o status do código (com e sem `details`); erro comum vira `500 INTERNAL_ERROR` com mensagem genérica, sem `message` original, SQL nem stack no corpo, e o log de nível `error` contém o erro completo com o `X-Request-Id` da resposta; handler `async` que rejeita chega ao tratamento central; com headers já enviados delega com `next(err)`; qualquer rota sem correspondência (GET e POST) recebe `404 NOT_FOUND`. Verificar com os testes do `api`.
- [x] 4.5 Criar `apps/api/src/handlers/products.handler.ts` e `apps/api/src/routes/products.routes.ts` com um teste do handler usando um service **fake**: usa os valores de `res.locals.validated.query`, chama o service uma vez e responde `200` com o resultado; a rota `GET /` aplica `validate` antes do handler. Verificar com os testes do `api` e `npm run typecheck -w @vynyl/api`.

## 5. Aplicação e bootstrap

- [x] 5.1 Criar `apps/api/src/app.ts` (`createApp({ db, logger })`: `x-powered-by` desabilitado, request logger, `/api/products`, not-found e error-handler, nessa ordem) e o ajudante `apps/api/src/test/app.ts`, com testes de rota via Supertest e o data set real cobrindo **todos os cenários** dos specs `product-listing-api` (30 padrão e `id` 1–30, `total` 44, representação do produto 1, preço decimal, páginas `limit=10&offset=10`, `offset=30`, `limit=100`, `offset=1000`, catálogo vazio, cada 400 e o `details` do parâmetro, busca `flux`/`FLUX`, só descrição, busca + paginação, sem resultados, `q=%25`, `q` vazio e com espaços), `api-error-handling` (404 em `/api/does-not-exist` e `/qualquer-coisa`, envelope e `Content-Type`, 500 real fechando o `client` antes da requisição sem vazar texto do erro) e `api-request-logging` (cabeçalho `X-Request-Id`, ausência de `x-powered-by`). Verificar com `npm test -w @vynyl/api` e mapear cada cenário dos specs a ao menos um teste (listar o mapeamento no relatório).
- [x] 5.2 Implementar `apps/api/src/server.ts` como raiz de composição (D2/D3): `getPaths` → `.env` opcional via `process.loadEnvFile` (ignora `ENOENT`, não sobrescreve o ambiente) → `loadConfig` (em `ConfigError`, escreve a mensagem em `stderr` e sai com código 1 antes de tocar no banco) → `createLogger` → `createDatabase` → `runMigrations` → `seedProducts` → `createApp` → `listen` (log `info` com a porta); falhas de inicialização são registradas em nível `fatal` e saem com código 1. Remover os `.gitkeep` das pastas `config`, `routes`, `handlers`, `services`, `repositories`, `mappers`, `middleware` e `lib` (agora com conteúdo). Verificar com `npm run typecheck`, `npm run lint` e `npm test` na raiz, e que nenhum `.gitkeep` restou em pastas com arquivos.

## 6. Verificação em runtime (o servidor é executado, não só testado)

- [x] 6.1 Iniciar o servidor de desenvolvimento (`tsx`) com `DATABASE_PATH` absoluto num diretório temporário do scratchpad, `PORT` livre e `LOG_LEVEL=info`, e chamar com `fetch`/`curl`: `GET /api/products` (30 itens, `total` 44), `?limit=10&offset=10`, `?q=flux`, `?q=%25` (0 resultados), `?limit=0` (400 com envelope e `details`), `GET /api/nope` (404), e o cabeçalho `X-Request-Id`; mostrar os resultados reais e uma linha de log. Encerrar o processo e confirmar a porta livre.
- [x] 6.2 Reiniciar o servidor sobre o **mesmo** arquivo de banco e verificar que `total` continua igual ao do arquivo do data set (sem duplicação) e que o log registra o seed como ignorado; comparar a contagem via `GET /api/products?limit=100`.
- [x] 6.3 Executar `npm run build -w @vynyl/api` e rodar `node apps/api/dist/server.js` **a partir de `apps/api` e depois a partir da raiz**, com `DATABASE_PATH=./data/t2-cwd-check.db`: confirmar que ambos usam o **mesmo** arquivo em `<raiz>/data/` (e que não surge `apps/api/data/`), que `dist/server.js` não contém importações de `@vynyl/shared` (embutido) e que a resposta de `GET /api/products` é igual à do modo dev. Remover os arquivos de banco temporários.
- [x] 6.4 Configuração inválida: iniciar com `PORT=abc` e depois com `PORT=abc LOG_LEVEL=verbose`; verificar código de saída diferente de zero, mensagem sem stack que nomeia cada variável inválida e que **nenhum** arquivo de banco foi criado.
- [x] 6.5 `.env` opcional: confirmar antes que não existe `.env` na raiz (se existir, pausar); criar um `.env` temporário com `PORT` e verificar que a API escuta nessa porta; definir `PORT` diferente no ambiente e verificar que o ambiente tem precedência; remover o `.env` temporário e confirmar que a API inicia normalmente sem ele.

## 7. Fechamento

- [x] 7.1 Executar na raiz `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` e `npm run format`; verificar código de saída 0 em todos, que `format` rodado duas vezes não altera arquivos e registrar a contagem de arquivos e testes por pacote.
- [x] 7.2 Conferir o escopo: `git status` só com arquivos previstos; `package.json` de cada workspace e `package-lock.json` **sem alterações** (nenhuma dependência nova); nenhum código fora de escopo (`express.json`, rate limit, `/health`, `SIGTERM`, singleflight, ordenação, categorias); nenhum `console.*`, TODO, arquivo temporário ou `data/*.db` versionável; `README.md`, `PROJECT_GUIDE.md`, `CLAUDE.md` e `DELIVERABLES.md` sem alterações.
- [x] 7.3 Executar `openspec validate api-list-products --strict` e verificar que passa. Refletir o progresso no `OPENSPEC_TASKS.md`: marcar `[x]` somente os itens de escopo e de critérios de aceite da T2 **de fato verificados**, **sem** marcar a T2 no Status geral nem o "Fechamento" (revisão, "Padrões aprovados", commit e notas do `AI.md` são do usuário).
- [x] 7.4 Entregar o relatório final no formato do `CLAUDE.md`: o que foi feito, comandos executados e resultados reais, o mapeamento cenário→teste, o que **não** foi verificado, decisões e premissas (incluindo o `.env` opcional e o schema único de produto), riscos observados e a lista dos **pontos de revisão** da seção do `design.md` para o usuário aprovar os padrões; sugerir a mensagem de commit `feat(api): add paginated product listing with search`. **Não fazer commit nem push.**
