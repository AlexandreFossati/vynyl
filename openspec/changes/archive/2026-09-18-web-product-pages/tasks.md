# Tasks

> References: specs in `specs/` (`product-detail`, `product-editing`, `product-deletion`, `web-notifications`, `web-app-structure`, `web-http-client`, `product-dashboard`); decisions in `design.md` (D1–D10); UI rules in the guide (section 10). Language: code, comments, interface texts and tests in **English**; these tasks in Portuguese. **Follow the frontend "Approved patterns"** (T5). **No new dependency**, no change in `apps/api` or `packages/shared`. Verification temporaries in the scratchpad. Tests follow the Definition of Done in `CLAUDE.md`.

## 1. Routes, formatting and API

- [x] 1.1 Create `lib/routes.ts` (D1) and `lib/format.ts` (D8: `formatCategory` moved from `ProductList`, plus `formatDateTime`), with tests (`routes.test.ts`, `format.test.ts`): all routes, `/products/new` before `:id`, invalid ids (`abc`, `0`, `-1`, `1.5`, `007`), trailing slash, path builders. Verify with the `web` tests.
- [x] 1.2 Extend `lib/api/products-api.ts` (D3) with `get`, `create`, `update` and `remove` and the `parse` helper, and type `DashboardPage` with `Pick<ProductsApi, 'list'>`; tests in `products-api.test.ts`: path, method and body of each operation, signal forwarded, response outside the contract → `INVALID_RESPONSE`, `remove` with no value, `ApiError` `409` and `400` forwarded. Verify with the `web` tests and `typecheck`.

## 2. Atoms and molecules

- [x] 2.1 Create `Link` and `Textarea` in `components/atoms` (D2, D5) and use `Link` in `NotFoundPage`; `Link` tests: a plain click navigates without reloading and without following the `href`, a click with Ctrl/Cmd/Shift/Alt and the middle button are not intercepted, variants. Verify with the `web` tests, including the existing `App.test.ts`.
- [x] 2.2 Create `FormField` and `Toast` in `components/molecules` (D5, D7); tests: label associated with the control (`getByLabelText`), `multiline` variant, error linked by `aria-describedby` and `aria-invalid`, without an error there are no such attributes; `Toast` with `role="alert"` only on error and "Dismiss notification" button calling the callback. Verify with the `web` tests.

## 3. State and form libraries

- [x] 3.1 Create `lib/toasts.svelte.ts` (D7) with tests (`toasts.test.ts`, fake timers): `success`/`error` add, automatic closing at 5 s and 8 s, `dismiss` removes only the requested toast and cancels the timer, several simultaneous toasts. Verify with the `web` tests.
- [x] 3.2 Create `lib/product-form.ts` (D5) with tests (`product-form.test.ts`): empty values and from a product (`9.99`, `1299`), "Required" on blank (including only spaces), "Enter a number" for `abc`/`1e2`/`9,99`, each schema rule (price with 3 decimal places, negative or fractional stock, category with uppercase letters, invalid SKU, long title), simultaneous errors, trimmed and converted values in the `input`, first letter of the message capitalized, `describeSaveFailure` (`SKU_CONFLICT`, `VALIDATION_ERROR` with known and unknown paths, other codes). Check the real Zod messages and note the ones that sound technical. Verify with the `web` tests.
- [x] 3.3 Create `lib/product-loader.svelte.ts` (D4). The tests come through the pages (tasks 5.1 and 5.3), with an out-of-order response and `id` change. Verify with `typecheck`.

## 4. Organisms

- [x] 4.1 Create `ProductForm` (D5) with tests: valid submit calls `onsubmit` with converted and trimmed values; empty submit shows "Required" on all fields, does not call `onsubmit` and focuses the first one; invalid numeric field; `initialValues` fill the fields; `busy` disables the submit and swaps the label for "Saving…"; `showErrors` shows the message and focuses the field; editing a field clears its error; "Cancel" calls `oncancel`. Verify with the `web` tests.
- [x] 4.2 Create `ConfirmDialog` (D6) and the minimal `<dialog>` polyfill in `src/test/setup.ts`; tests: `open` opens and closes the dialog, title as accessible name, initial focus on "Cancel", `cancel` event (Esc) calls `oncancel` and does not close by itself, `busy` ignores Esc and disables the actions, confirming calls `onconfirm`. Verify with the `web` tests.
- [x] 4.3 Create `ProductDetail`, `Toaster` and adjust `Header` and `ProductList` (D8, D9); tests: `ProductDetail` (all fields, price, stock badge, `<time datetime>` of the dates), `Toaster` (`aria-live="polite"` region always present, one `Toast` per item, dismiss calls the callback), `ProductList` (title is a link to `/products/:id` in the cards and in the table; adjust the affected existing tests), `Header` (brand is a link to `/`). Verify with the `web` tests.

## 5. Pages and application

- [x] 5.1 Create `ProductDetailPage` (D8) with tests (fake API and fake `notify`): product displayed with "Back to products" and "Edit" links; loading; error with "Try again" (no technical details); `404` → "Product not found"; deletion: "Delete" opens the dialog without calling the API, "Cancel" closes, confirming calls `remove` once, success → toast and `/`, `404` → error toast and `/`, failure → dialog closes, error toast and stays on the detail page; `id` change with a delayed response. Verify with the `web` tests.
- [x] 5.2 Create `ProductCreatePage` with tests: empty form; invalid submit does not call the API; valid submit calls `create` with the values; success → toast "Product created" and navigates to `/products/<id>`; double submit generates a single call (pending promise); `409` → error on the SKU, values kept, button re-enabled; `400` with `details` → fields and toast for an unknown path; generic failure → toast and values kept; "Cancel" goes back to `/` without calling the API. Verify with the `web` tests.
- [x] 5.3 Create `ProductEditPage` with tests: form filled from the product; loading, error with "Try again" and `404`; success → `update` with all fields, toast "Product updated" and navigates to the detail page; unchanged own SKU is sent; `409`, `400` and generic failure as in creation; `404` on save → toast and `/`; "Cancel" goes back to the detail page. Verify with the `web` tests.
- [x] 5.4 Wire `App.svelte` (routes, `createToasts`, `Toaster`, `notify`) and add "Add product" to `DashboardPage`; tests: `App` (each route shows the right page, `/products/abc` shows not found without calling the API, navigation by link and by `popstate`, a toast created by a page appears and survives navigation), `DashboardPage` ("Add product" leads to `/products/new` and stays visible with an empty catalog, search with no results and error). Verify with the `web` tests.

## 6. Verification and closing

- [x] 6.1 Verification in a real browser (D10): real API with a temporary database + Vite with proxy + temporary Cypress spec in the scratchpad. Full flow through the UI (list → detail → create → edit → duplicate SKU → delete), Esc and dialog focus, toasts, and layout at 360, 768 and 1280 px (no horizontal scroll, form in 1 × 2 columns, full-screen dialog on mobile, visible focus), with screenshots read. State what was not verifiable.
- [x] 6.2 Run at the root `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` and `npm run format` (twice, idempotent); check the scope (`package.json`/lockfile and `apps/api`/`packages/shared` unchanged, no `console.*`/TODO, no literal colors in the components, no component below `pages` imports `lib/api`, no temporaries); `openspec validate web-product-pages --strict`; reflect in `OPENSPEC_TASKS.md` only the scope and acceptance items of T6 actually verified; deliver the report with what was **not** verified.
